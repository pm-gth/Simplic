# Program that prints all the even numbers between X and UPPER_BOUND

SET X = 0
SET UPPER_BOUND = 30

WHILE X LT UPPER_BOUND DO
        IF X % 2 EQ 0 AND X NEQ 0 THEN # Zero is not even
                PRINTLN X + " is even"
        FI
        INCR X
DONE
